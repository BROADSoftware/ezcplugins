
MYDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

aws ec2 stop-instances --instance-ids $(aws ec2 describe-instances --query 'Reservations[].Instances[].InstanceId' --filters "Name=tag:Cluster,Values={{m.cluster.id}}" "Name=instance-state-name,Values=running" --output text)

