
type: map
mapping:
  "repositories":
    type: map
    required: true
    mapping:
      "jdk_oracle": 
        type: seq
        required: false
        sequence:
        - type: map
          mapping:
            "repo_id": { type: str, required: True }
            "rpm_url": { type: str, required: True }
            "jce_archive": { type: str, required: True }
            "jce_url": { type: str, required: True }
                      
    
    
    
  
